{
  "config": {
    "step": {
      "user": {
        "title": "Set up Deterministic Agent",
        "description": "Configure the Deterministic orchestrator endpoint used by Assist.",
        "data": {
          "orchestrator_url": "Orchestrator URL",
          "request_timeout": "Request timeout (seconds)"
        }
      }
    },
    "error": {
      "invalid_url": "URL must start with http:// or https://"
    },
    "abort": {
      "single_instance_allowed": "Only one Deterministic Agent instance is allowed"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Deterministic Agent options",
        "data": {
          "orchestrator_url": "Orchestrator URL",
          "request_timeout": "Request timeout (seconds)"
        }
      }
    },
    "error": {
      "invalid_url": "URL must start with http:// or https://"
    }
  }
}
